import { ChatMessage } from "./chat-message";
import { CardContent } from "./ui/card";

export const ChatMessages = () => {
    return (
        <CardContent>
            <ChatMessage />
        </CardContent>
    );
}